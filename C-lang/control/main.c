//  decision making if, if else, else if, switch
// to check the number is even or odd, 342 usng if statement                                                                                   
 
#include <stdio.h>  // header file

int main() {     // main block ...entry point
	int a = 341;
	
	if(a%2==0){   // if block here we can test one or more than one condition
		printf("The number is even");  // statement
	} else {
		printf("the number is odd ");
	}
	
}
