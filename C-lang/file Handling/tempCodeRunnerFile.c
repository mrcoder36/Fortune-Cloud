FILE *fp;
    // int length;
    // fp = fopen("file22.txt", "r");

    // fseek(fp, 0, SEEK_END);

    // length = ftell(fp);
    // printf("The Length of file is %d bytes", length);
    // fclose(fp);