#include<stdio.h>

int main() {
    int age = 81;

    if(age >= 18 && age <= 80) {
        printf("You can drive a Vehicle");
    } else {
        printf("You are not eligible to drive a vehicle");
    }
}